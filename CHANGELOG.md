# 版本记录

## v0.1.0 - 2026-08-16

首个完成双向联调的 HTTP 版本。

### 新增

- QQ 群聊和私聊消息转换为标准 JSON，并通过异步 HTTP Webhook 转发。
- 本地 HTTP API 向指定 QQ 群聊或私聊发送文字和图片。
- 基于会话类型、会话 ID、发送者、文本前缀、正则和图片的路由条件。
- 会话地址自动记录、WebUI 编辑，以及人工别名兜底。
- 文件式自定义 Webhook JSON 模板。
- HTTP Bearer 鉴权、请求体限制、超时、重试和结构化错误响应。
- 投递统计、投递历史、失败记录和路由诊断命令。
- 图片和媒体缓存的大小、类型、超时与路径安全限制。

### 修复

- 统一 QQ 来源配置，移除重复的接收 ID 和群聊 ID 逻辑。
- QQ 入站路由不再错误依赖手动会话绑定。
- 修复 AstrBot 移除 `/` 唤醒前缀后，群聊文本前缀永远无法命中的问题。
- 清理未实现的 WebSocket 开关、重复路由模式和在线 JSON 编辑器。

### 范围

- 本版本只发布 HTTP 双向通信。
- WebSocket 留待后续独立里程碑实现，不属于 `v0.1.0`。
